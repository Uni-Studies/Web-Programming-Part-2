FrontEnd for ProgressTracker

Run:
1. npm install
2. npm run dev

Notes:
- Uses fetch API and localStorage for auth token
- baseURL is in src/api/config.js
- SB Admin 2 assets loaded from CDN in index.html
